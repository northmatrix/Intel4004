so i want to make a cpu my own with

8 Registers

ax return address
bx base address 
dx data address
cx counter address
di destination
si source
sp stack pointer
bp base pointer


Instructions

mov dx, ax      reg -> Reg
mov dx, 0xF     reg -> Mem 

mov 0x01, ax    imm -> Reg 
mov 0x01, [di]  imm -> Mem

mov si, [di]    reg -> mem
mov [si] di     mem -> reg


Now xor,and,or, operate the exact same ways

xor, 0x01, ax     imm -> reg 
xor, 0x01, [ax]   imm -> mem
xor, 
